# APP_URL: The URL where this app is hosted.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# MongoDB connection URI (e.g. mongodb+srv://username:***@cluster.mongodb.net/hxcfd?retryWrites=true&w=majority)
MONGODB_URI=
MONGODB_DB_NAME="hxcfd"

# SMTP Email Configuration (Optional for real email dispatch)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="HX Engineering <noreply@hx-engineering.com>"